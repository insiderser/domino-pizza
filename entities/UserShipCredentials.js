class UserShipCredentials {
    constructor(phone, email, firstName, lastName, address) {
        this.phone = phone
        this.email = email
        this.firstName = firstName
        this.lastName = lastName
        this.address = address
    }
}

export default UserShipCredentials
